rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule: Allow read access to everyone (public scoreboard)
    // Only allow write access to authenticated users (admins)
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
